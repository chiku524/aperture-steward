{
  "name": "aperture-steward-agent",
  "version": "0.1.0",
  "description": "Aperture Steward — personal cognitive-load ElizaOS agent (Nosana + steward UI)",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "packageType": "project",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "start": "elizaos start",
    "dev": "elizaos dev",
    "verify:deploy": "node scripts/verify-endpoints.mjs",
    "nosana:deployments": "node scripts/nosana-deployment.mjs",
    "setup:creds": "node scripts/setup-credentials.mjs"
  },
  "dependencies": {
    "@elizaos/core": "^1.0.0",
    "@elizaos/plugin-bootstrap": "^1.0.0",
    "@elizaos/plugin-openai": "^1.0.0"
  },
  "devDependencies": {
    "@elizaos/cli": "^1.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
